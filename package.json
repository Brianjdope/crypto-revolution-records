{
  "name": "crypto-revolution-records",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Crypto Revolution Records — single-page brand site with real Stripe + Apple/Google Pay onramp.",
  "scripts": {
    "dev": "node dev-server.mjs",
    "stripe-listen": "stripe listen --forward-to http://localhost:5180/api/stripe/webhook",
    "stripe-trigger": "stripe trigger payment_intent.succeeded"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "stripe": "^17.3.1"
  }
}
